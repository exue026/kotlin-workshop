package com.zeitspace.kotlin.activity2

object Activity2 {

    fun add(x: Int, y: Int) = 0/* create an inline function for addition */

    val subtract = 0/* create a function to subtract two ints*/

    fun performBinaryOperation(): Int {
        /* create a higher-order function that takes two operands and an operator function
           then returns the result of applying the operator function */
        return 0
    }

}